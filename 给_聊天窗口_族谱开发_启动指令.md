# 给「聊天窗口：族谱开发」的启动指令

> 项目：智能体族谱SAAS
> 任务来源：木哥
> 当前状态：最终交付包已整理完成，要求进入正式开发阶段。

## 1. 已启用规则清单

- 商业全栈交付大师
- 智能体 / Agent / RAG 框架规则
- PostgreSQL 16 + pgvector 规则
- Redis 缓存 / 限流 / 幂等规则
- SaaS 多租户 / 授权 / 审计规则
- 私有化交付 / 部署 / 安全规则

## 2. 默认假设

- 后端技术栈：Go + Gin
- 数据库：PostgreSQL 16 + pgvector
- 缓存：Redis 7
- 文件存储：MinIO / S3 兼容
- SaaS 能力：tenant 隔离、kb 隔离、审计日志、资源限制预留
- RAG 能力：向量检索 + 全文检索 + Trigram 混合检索
- 当前阶段优先开发后端 MVP，不先扩展前端。

## 3. 必读入口

请先阅读以下文件：

1. `README.md`
2. `03_智能体族谱SAAS_开发方案.md`
3. `04_最终交付验收清单.md`
4. `02_docs_v1_delivery/01_开发任务清单.md`
5. `02_docs_v1_delivery/03_数据库表结构_v1.sql`
6. `02_docs_v1_delivery/04_API接口清单.md`
7. `02_docs_v1_delivery/05_Redis_Key设计.md`
8. `01_backend/README.md`
9. `01_backend/向量存储与索引优化说明.md`

## 4. 当前代码基线

后端目录：

```text
01_backend/
```

已具备：

- Gin 服务骨架；
- `/healthz` 健康检查；
- `/readyz` 就绪检查；
- Request-ID 中间件；
- 生产 Release Mode；
- PostgreSQL 连接；
- KB 向量 / 混合检索基础代码；
- PostgreSQL HNSW cosine 索引迁移；
- 全文检索 / Trigram 检索迁移；
- tenant_id / kb_id 隔离字段；
- profile 字段；
- 1536 维 embedding 校验；
- 迁移脚本 up/down；
- `go test ./...` 已通过。

## 5. 立即开始的开发任务

### P0-1：补齐认证与租户基础

目标：完成 SaaS 后端最小可用鉴权链路。

需要实现：

- `internal/module/auth`
  - 用户注册；
  - 用户登录；
  - JWT 签发；
  - JWT 中间件；
  - 当前用户信息接口；
- `internal/module/tenant`
  - 租户创建；
  - 当前用户租户列表；
  - 当前租户上下文识别；
- 密码必须使用 bcrypt / argon2；
- JWT Secret 从环境变量读取，不允许硬编码；
- API 响应复用 `pkg/response`。

建议接口：

```text
POST /api/v1/auth/register
POST /api/v1/auth/login
GET  /api/v1/auth/me
GET  /api/v1/tenants
POST /api/v1/tenants
```

### P0-2：补齐 KB 文档写入链路

目标：让知识库可写、可检索。

需要实现：

- 创建 KB；
- KB 列表；
- 创建文档；
- 创建 chunk；
- 写入 embedding；
- 检索接口接入 tenant / kb 权限校验。

建议接口：

```text
POST /api/v1/kbs
GET  /api/v1/kbs
POST /api/v1/kbs/:kb_id/documents
POST /api/v1/kbs/:kb_id/chunks
POST /api/v1/kbs/:kb_id/search
```

### P0-3：补齐基础测试

需要覆盖：

- 配置加载；
- JWT 签发与解析；
- Request-ID；
- KB search 参数校验；
- embedding 维度校验；
- repository SQL 构建边界。

验收命令：

```bash
cd 01_backend
go test ./...
```

## 6. 开发约束

- 不破坏现有目录结构；
- 不删除 migrations；
- 所有数据库结构变更必须新增 migration，包含 up/down；
- 不把密钥写入代码；
- 不引入不必要大框架；
- 不降低 tenant_id / kb_id 隔离；
- 每完成一个 P0 任务，必须更新文档与验收清单；
- 每次提交前必须执行 `go test ./...`。

## 7. 第一轮交付物

请完成并回传：

1. 代码变更清单；
2. 新增 / 修改的迁移脚本；
3. 新增 API 列表；
4. 环境变量清单；
5. 测试结果；
6. 下一步建议。

## 8. 一句话执行命令

请「聊天窗口：族谱开发」从这里开始：

```text
进入 `智能体族谱SAAS_最终交付包/01_backend`，基于现有 Go/Gin/PostgreSQL/pgvector 项目，优先开发 P0-1 认证与租户基础，然后开发 P0-2 KB 写入链路，最后补齐 P0-3 测试。所有变更必须包含 migration、文档更新和 go test ./... 结果。
```
