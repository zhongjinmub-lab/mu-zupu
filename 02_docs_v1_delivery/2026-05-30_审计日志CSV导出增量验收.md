# 2026-05-30 审计日志 CSV 导出增量验收

## 范围

- 在现有审计日志筛选分页能力基础上增加 CSV 导出。
- 导出按当前租户隔离，支持复用筛选条件。
- 本增量不新增数据库表。

## 后端

- [x] 新增 `GET /api/v1/audit-logs/export`。
- [x] 支持 `action`、`resource_type`、`actor_user_id`、`from`、`to` 查询参数。
- [x] 导出固定按当前 `X-Tenant-ID` 租户上下文查询。
- [x] CSV 字段包含 `id`、`tenant_id`、`actor_user_id`、`action`、`resource_type`、`resource_id`、`ip`、`user_agent`、`metadata`、`created_at`。
- [x] 单次导出最多 1000 条，避免大结果集拖垮接口。

## 前端

- [x] 审计日志筛选区域增加“导出 CSV”按钮。
- [x] 导出复用当前筛选表单条件。
- [x] 使用当前登录 Token 和 `X-Tenant-ID` 下载 CSV 文件。

## 验证

- [x] `gofmt` 已执行。
- [x] `go test ./...` 通过。
- [x] `D:\Node.js\node.exe --check 02_frontend\assets\app.js` 通过。
