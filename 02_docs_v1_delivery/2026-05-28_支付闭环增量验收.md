# 2026-05-28 支付闭环增量验收

## 变更范围

- 新增 `000014_payment_lifecycle_audit` up/down migration。
- 新增 `payment_callback_events` 支付回调审计表。
- 新增订单取消/关闭接口。
- 新增支付单列表/关闭接口。
- 新增支付回调审计事件列表接口。
- 管理台 `/saas/` 新增支付单列表、回调审计、订单取消/关闭和支付单关闭入口。

## 新增接口

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/v1/orders/:order_id/cancel` | 取消当前租户 pending 订单，并关闭关联 pending 支付单 |
| POST | `/api/v1/orders/:order_id/close` | 关闭当前租户 pending 订单，并关闭关联 pending 支付单 |
| GET | `/api/v1/payment-orders` | 当前租户支付单列表，支持 `business_order_id` 过滤 |
| POST | `/api/v1/payments/:payment_id/close` | 关闭当前租户 pending/failed 支付单 |
| GET | `/api/v1/payment-callback-events` | 当前租户支付回调审计事件，支持 `pay_no` 过滤 |

## 验收结果

- [x] 本地 `go test ./...` 通过。
- [x] 线上 migration 已应用：`000014_payment_lifecycle_audit`。
- [x] 线上 `mu-agent-saas` active。
- [x] 线上 `mu-agent-document-worker` active。
- [x] 线上 `/saas-api/api/v1/ready` 返回 200。
- [x] 线上管理台 JS 包含支付单列表、关闭和回调审计接口。
- [x] 真实线上回归通过：订单取消后关联支付单变为 `closed`。
- [x] 真实线上回归通过：支付单可单独关闭为 `closed`。
- [x] 真实线上回归通过：订单可关闭为 `closed`。
- [x] 真实线上回归通过：mock 支付回调后支付单为 `paid`，查单为 `paid`。
- [x] 真实线上回归通过：支付回调审计事件记录成功，`result_status=paid`。

## 线上回归样本

```json
{
  "tenant_id": "67b7dcdc-cb9e-483e-bf57-efc42446af5d",
  "cancel_order_status": "cancelled",
  "cancel_payment_status": "closed",
  "close_payment_status": "closed",
  "close_order_status": "closed",
  "callback_payment_status": "paid",
  "query_paid_status": "paid",
  "callback_event_count": 1,
  "callback_event_result": "paid"
}
```
