# 2026-05-28 管理台审计日志筛选分页增量验收

## 变更范围

- 管理台审计日志面板接入后端 `/api/v1/audit-logs` 筛选分页能力。
- 静态前端包：`02_frontend/dist-frontend-audit-pagination.tar.gz`。

## 页面能力

管理台入口：

```text
https://zupu.jiangxinnet.com/saas/
```

审计日志面板新增：

- `action` 筛选。
- `resource_type` 筛选。
- `actor_user_id` 筛选。
- `from` / `to` 时间筛选。
- `limit` 每页数量选择。
- 上一页 / 下一页 cursor 分页。
- 分页状态显示。
- 审计记录补充显示 `actor_user_id` 与 `ip`。

## 线上验证

- `https://zupu.jiangxinnet.com/saas/` HTML 已包含：
  - `auditFilterForm`
  - `auditPrevBtn`
  - `auditNextBtn`
  - `auditPagerInfo`
- `https://zupu.jiangxinnet.com/saas/assets/app.js` 已包含：
  - `auditFiltersFromForm`
  - `auditQuery`
  - `auditNextCursor`
  - `auditPrevBtn`
  - `auditNextBtn`
- `https://zupu.jiangxinnet.com/saas-api/api/v1/ready` 返回 `ok`。

## 安全说明

- 前端只传递后端已支持的查询参数，不传递或覆盖 `tenant_id`。
- 租户隔离继续由后端当前登录态与 `X-Tenant-ID` 校验保证。
- 本次部署未写入服务器密码、私钥、token 到开发产物。
