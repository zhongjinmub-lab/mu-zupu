# 2026-05-29 Agent 编辑管理台增量验收

## 管理台

- 智能体列表新增“编辑”操作；
- 点击编辑后回填名称、编码、描述和系统提示词；
- 编辑模式下编码只读，提交调用 `PUT /api/v1/agents/:agent_id`；
- 取消编辑后恢复为创建模式；
- 创建模式继续调用 `POST /api/v1/agents`。

## 行为约束

- 请求继续携带 `Authorization: Bearer <token>` 和 `X-Tenant-ID: <tenant_id>`；
- 更新继续复用后端 `tenant_id + agent_id` 隔离；
- 已发布 Agent 更新后由后端回到 `draft` 状态；
- 本增量不新增数据库结构，不新增后端接口。

## 本地验证

- `go test ./...` 通过；
- 前端静态文件为免构建交付，已做 JavaScript 语法检查。
