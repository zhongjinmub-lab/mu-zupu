# 2026-05-28 管理台中文显示修复增量验收

## 变更范围

- 重写 `02_frontend/index.html` 为正常 UTF-8 简体中文页面。
- 重写 `02_frontend/assets/app.js` 中所有可见文案，保留原有管理台功能。
- 保留并修复审计日志筛选分页控件。
- 静态前端包：`02_frontend/dist-frontend-clean-cn.tar.gz`。

## 已修复问题

- 页面标题乱码。
- 导航菜单乱码。
- 登录、注册、租户、知识库、智能体、授权、用量、订单、支付、成员、邀请、审计日志等可见文案乱码。
- 部分表单 placeholder 和按钮文案乱码。
- 审计分页状态文案乱码。

## 线上验证

线上入口：

```text
https://zupu.jiangxinnet.com/saas/
```

已验证 HTML 包含正常中文：

- `智能体族谱 SAAS 管理台`
- `登录管理台`
- `审计日志`
- `筛选`
- `上一页`
- `下一页`

已验证 JS 包含正常中文：

- `暂无审计日志`
- `第 ${state.auditPage} 页 / ${state.auditLogs.length} 条`
- `License 验证通过`

后端 ready：

```text
https://zupu.jiangxinnet.com/saas-api/api/v1/ready
```

返回 `ok`。

## 安全说明

- 本次只更新管理台静态文件，不修改后端鉴权和租户隔离逻辑。
- 部署过程中临时 SSH key 写入 `%TEMP%`，部署后已清理。
- 未写入服务器密码、私钥、token 到开发产物。
