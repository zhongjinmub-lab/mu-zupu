# 2026-05-31 后端运维入口文档同步增量验收

## 目标

在生产脚本持续增强后，同步后端入口 README 的生产脚本摘要，避免开发和运维人员只看 `01_backend/README.md` 时遗漏配置归档恢复、前端静态资源备份和公网冒烟检查能力。

## 变更

- `01_backend/README.md` 新增 `restore-config.sh` 说明。
- `01_backend/README.md` 更新 `smoke.sh` 说明，明确覆盖公网 API、前端静态资源、Nginx 配置、服务和迁移状态。
- release 脚本测试新增对后端 README 的运维脚本说明校验。

## 验收

- [x] 后端 README 提到 `restore-config.sh`。
- [x] 后端 README 提到公网 API 和前端静态资源冒烟检查。
- [x] 后端 README 提到 Nginx 配置检查。
- [x] 单元测试覆盖后端 README 运维入口说明。
- [x] 本轮不部署服务器，部署验证留到后续明确要求时执行。
