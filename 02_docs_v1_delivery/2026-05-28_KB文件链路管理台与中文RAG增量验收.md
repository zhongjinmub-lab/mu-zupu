# 2026-05-28 KB 文件链路管理台与中文 RAG 增量验收

## 变更范围

- 管理台知识库页新增文件写入链路入口：
  - 文件上传。
  - 文件列表。
  - 文档任务创建。
  - 文档任务执行。
  - 待向量化 chunk 列表。
  - embedding run。
- 后端混合检索新增中文关键词 fallback：
  - 原向量/全文检索无结果时启用。
  - 仅在当前 `tenant_id + knowledge_base_id` 范围内查找。
  - 只匹配已向量化且未删除的 chunk。

## 线上入口

```text
https://zupu.jiangxinnet.com/saas/
```

已验证页面包含：

- `文件上传`
- `文档任务`
- `待向量化 Chunk`
- `执行向量化`

## 真实链路验证

线上 API 回归步骤：

1. 注册测试用户。
2. 创建测试租户。
3. 创建测试知识库。
4. 上传 `.txt` 文件。
5. 创建 `parse_chunk` 文档任务。
6. 执行 `/document-jobs/run`。
7. 查询 pending chunks。
8. 执行 `/embedding/run`。
9. 使用 `/ask` 提问中文问题。

验证结果：

- 文档任务处理成功：`processed=1`。
- 向量化处理成功：`processed=1`。
- 向量化后 pending chunks：`0`。
- 中文 RAG 问答返回引用：`references=1`。
- 命中片段包含：`李四是张三的后代`。

## 安全与隔离

- 前端仍只传当前登录态和 `X-Tenant-ID`。
- 后端 fallback 查询继续强制 `tenant_id` 与 `knowledge_base_id` 条件。
- 不接受客户端覆盖 `tenant_id`。
- 本次后端变更不需要 migration。
- 未写入服务器密码、私钥、token 到开发产物。
