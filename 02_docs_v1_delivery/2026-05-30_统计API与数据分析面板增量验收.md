# 2026-05-30 统计 API 与数据分析面板增量验收

## 范围

- 新增当前租户统计 API。
- 管理台新增“分析”页面，展示资源、经营、用量趋势、最近操作和风险项。
- 本增量不新增数据库表，不改变既有租户隔离模型。

## 后端

- [x] 新增 `internal/module/analytics` 模块。
- [x] 新增 `GET /api/v1/analytics/summary`。
- [x] 接口固定读取当前 `X-Tenant-ID` 租户上下文。
- [x] 返回知识库、文件、文档、Chunk、智能体、会话、成员和邀请计数。
- [x] 返回订单、支付、License 状态分布和已支付收入。
- [x] 返回近 7 天用量趋势、最近审计操作和运营风险项。

## 前端

- [x] 顶部导航新增“分析”入口。
- [x] 新增数据分析面板。
- [x] 支持刷新 `/analytics/summary`。
- [x] 展示核心指标、资源结构、订单与授权分布、近 7 天用量趋势、最近操作和风险项。

## 验证

- [x] `gofmt` 已执行。
- [x] `go test ./...` 通过。
- [x] `D:\Node.js\node.exe --check 02_frontend\assets\app.js` 通过。
