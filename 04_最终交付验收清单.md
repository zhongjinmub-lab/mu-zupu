# 智能体族谱SAAS 最终交付验收清单

## 1. 已交付

- [x] 后端源码；
- [x] Go module；
- [x] Docker Compose 基础依赖；
- [x] PostgreSQL/pgvector 迁移；
- [x] 健康检查接口；
- [x] 就绪检查接口；
- [x] 请求 ID 中间件；
- [x] KB 向量检索接口；
- [x] KB 混合检索接口；
- [x] HNSW、全文、Trigram 索引；
- [x] 检索 Profile 表；
- [x] 检索日志表；
- [x] 用户注册、登录、JWT 签发与解析；
- [x] JWT 中间件与当前用户接口；
- [x] 租户创建、租户列表与当前租户上下文；
- [x] 知识库创建、列表、文档创建、chunk 写入；
- [x] 文件上传到 MinIO/S3 与文件列表接口；
- [x] 已上传文本文件生成 KB 文档与 pending chunks；
- [x] file-backed 文档切片重建接口；
- [x] 数据库 document_jobs 队列和同步 worker run 接口；
- [x] 后台常驻文档 worker 进程与 systemd 模板；
- [x] pending chunks 查询与单 chunk embedding 写回；
- [x] 本地 deterministic embedding provider 与同步 embedding run 接口；
- [x] OpenAI-compatible HTTP embedding provider 可配置接入；
- [x] OpenAI-compatible HTTP generation provider 可配置接入；
- [x] 1536 维 embedding 写入校验；
- [x] 检索接口接入 tenant/kb 权限校验；
- [x] RAG 问答生成闭环接口；
- [x] Agent 基础管理接口；
- [x] Agent 与知识库绑定/解绑接口；
- [x] Agent 测试会话接口；
- [x] Agent 多轮会话编排、会话列表和消息列表接口；
- [x] Agent SSE 流式对话接口和管理台流式发送入口；
- [x] 智能体族谱图谱、关系维护、筛选、结构诊断和 CSV 导出；
- [x] 套餐/订阅/用量记录 MVP，支持租户用量汇总；
- [x] 套餐额度硬限制 MVP，覆盖文件上传、embedding、RAG 和 Agent 消息；
- [x] 订单/支付 MVP，支持业务订单、mock 支付单、支付查询和回调幂等；
- [x] 支付回调 HMAC 验签；
- [x] License 授权 MVP，支持租户内创建、列表、激活和吊销；
- [x] License 离线签名验签 MVP，支持 Ed25519 公钥配置、验证接口和激活前验签；
- [x] Webhook 配置、测试发送、投递记录、重试 Worker、状态摘要和 CSV 导出；
- [x] API 限流，支持内存限流和 Redis 可选限流；
- [x] 通用审计、审计筛选分页和 CSV 导出；
- [x] 敏感字段保护摘要，Webhook secret 和 License signature 响应脱敏；
- [x] 前端管理台，覆盖登录、租户、知识库、Agent、族谱、License、用量订单、Webhook、审计和设置；
- [x] 内置 migration CLI，支持 up/down/status；
- [x] 服务器部署、Nginx HTTPS 反代、systemd 守护；
- [x] 服务器冒烟脚本和每日备份 timer；
- [x] 生产部署模板已固化到 `deploy/production/`；
- [x] 生产升级、回滚、数据库恢复和恢复演练脚本；
- [x] Linux amd64 release 打包脚本；
- [x] 开发方案；
- [x] API 文档；
- [x] Redis Key 文档；
- [x] MVP 排期；
- [x] 交付验收文档。

## 2. 已验证

- [x] `go test ./...` 通过；
- [x] `node --check 02_frontend/assets/app.js` 通过；
- [x] 配置加载、JWT、Request-ID、KB 参数、embedding 维度、tenant/kb 隔离测试通过；
- [x] License 参数校验和租户隔离 SQL 条件已覆盖；
- [x] Webhook、SSE、限流、审计、敏感字段摘要和运维脚本测试通过；
- [x] 管理台静态入口 `/saas/`、CSS、JS 和后端 `/saas-api/ready` 线上访问通过；
- [x] 文档已精简；
- [x] 冗余 `.bak` 文件已清理；
- [x] manifest 已重新生成。

## 3. 后续增强建议

- 前端 Vue3/Vite 工程化迁移；
- 真实第三方支付渠道接入；
- 多实例生产环境 Redis 限流压测和监控告警增强。
## 2026-05-28 增量验收：审计日志筛选分页

- [x] `GET /api/v1/audit-logs` 支持 `action`、`resource_type`、`actor_user_id`、`from`、`to`、`limit`、`cursor` 查询参数。
- [x] 响应返回 `items` 与 `next_cursor`，继续复用 `pkg/response`。
- [x] 审计日志查询固定按当前 `X-Tenant-ID` 租户上下文隔离，不接受客户端覆盖 `tenant_id`。
- [x] 新增 `000017_audit_logs_filter_indexes` migration up/down。
- [x] 本地 `go test ./...` 通过。

## 2026-05-28 增量验收：KB 文档详情与 Chunk 列表

- [x] `GET /api/v1/kbs/:kb_id/documents/:document_id` 返回当前租户当前 KB 下的文档详情与 `file_id`。
- [x] `GET /api/v1/kbs/:kb_id/documents/:document_id/chunks` 返回当前租户当前 KB 下单文档 chunks。
- [x] 两个接口固定按 `tenant_id + kb_id + document_id` 隔离校验，不降低租户/知识库隔离。
- [x] 管理台文档列表增加文档详情与 Chunk 查看入口。
- [x] 本地 `go test ./...` 通过。

## 2026-05-29 增量验收：文件下载

- [x] `GET /api/v1/files/:file_id/download` 返回当前租户下已上传文件对象。
- [x] 下载接口固定按 `tenant_id + file_id` 隔离校验，不降低租户/文件隔离。
- [x] 管理台文件列表增加“下载”入口。
- [x] 本地 `go test ./...` 通过。

## 2026-05-29 增量验收：KB 文档归档

- [x] `DELETE /api/v1/kbs/:kb_id/documents/:document_id` 归档当前租户当前 KB 下文档。
- [x] 归档固定按 `tenant_id + kb_id + document_id` 隔离校验，不降低租户/KB 隔离。
- [x] 归档同事务软删除文档、该文档 chunks 和关联 document_jobs。
- [x] 管理台文档列表增加“归档”入口。
- [x] 本地 `go test ./...` 通过。

## 2026-05-29 增量验收：Agent 多轮会话管理台

- [x] 管理台智能体页增加多轮会话发送入口，调用 `POST /api/v1/agents/:agent_id/chat`。
- [x] 支持当前会话续聊和新会话切换。
- [x] 支持查看 Agent 会话列表和会话消息列表。
- [x] 发送后刷新会话、消息和用量汇总。

## 2026-05-29 增量验收：Agent 知识库绑定管理台

- [x] 管理台智能体页增加知识库绑定入口。
- [x] 支持绑定当前租户知识库到 Agent。
- [x] 支持查看 Agent 已绑定知识库列表。
- [x] 支持解绑 Agent 知识库。
- [x] 绑定/解绑继续复用后端租户与 KB 隔离校验。

## 2026-05-29 增量验收：Agent 归档管理台

- [x] 管理台智能体列表增加“归档”入口。
- [x] 归档前有确认提示。
- [x] 归档调用 `DELETE /api/v1/agents/:agent_id`。
- [x] 归档后刷新 Agent、绑定关系、会话和消息列表。
- [x] 归档继续复用后端 `tenant_id + agent_id` 隔离校验。

## 2026-05-29 增量验收：Agent 编辑管理台

- [x] 管理台智能体列表增加“编辑”入口。
- [x] 编辑时回填名称、编码、描述和系统提示词。
- [x] 编辑模式提交调用 `PUT /api/v1/agents/:agent_id`。
- [x] 支持取消编辑并恢复创建模式。
- [x] 更新继续复用后端 `tenant_id + agent_id` 隔离校验。

## 2026-05-29 增量验收：总后台布局参考图改造

- [x] 管理台切换为深色顶部横向导航布局。
- [x] 总览页增加授权提示条、租户信息卡、实时概况和右侧信息栏。
- [x] 总览页增加知识库数据和用户数据分区。
- [x] 保留原有后台操作入口。
- [x] 桌面和移动截图验证无横向溢出。

## 2026-05-29 增量验收：总后台排行与动态

- [x] 总览页新增知识库排行 TOP5。
- [x] 总览页新增智能体排行 TOP5。
- [x] 右侧信息栏新增产品动态。
- [x] 右侧信息栏新增更新日志。
- [x] 排行链接可跳转到对应管理模块。
- [x] 桌面和移动截图验证无横向溢出。

## 2026-05-29 增量验收：总后台真实数据总览 UI 优化

- [x] 租户卡显示当前租户编码和 License 到期聚合。
- [x] 右侧服务、待办和经营卡片改为读取已加载系统状态。
- [x] 实时动态从审计日志、文档任务和支付回调事件派生。
- [x] 系统洞察从知识库、文档、文件、智能体、会话、授权、用量和异常支付派生。
- [x] 首页面板、卡片、指标和活动流视觉层次已优化。
- [x] `node --check`、`go test ./...`、桌面和移动截图验证通过。

## 2026-05-30 增量验收：统计 API 与数据分析面板

- [x] 新增 `GET /api/v1/analytics/summary` 当前租户统计接口。
- [x] 统计接口聚合资源、经营、用量趋势、最近操作和风险项。
- [x] 管理台新增“分析”导航和数据分析面板。
- [x] 前端分析面板读取真实统计 API，不使用静态假数据。
- [x] `gofmt`、`go test ./...`、`node --check` 验证通过。

## 2026-05-30 增量验收：审计日志 CSV 导出

- [x] 新增 `GET /api/v1/audit-logs/export`。
- [x] 导出支持审计日志筛选条件并固定按租户隔离。
- [x] 管理台审计日志区域新增“导出 CSV”按钮。
- [x] 单次导出限制 1000 条，避免大结果集风险。
- [x] `gofmt`、`go test ./...`、`node --check` 验证通过。


## 2026-05-31 增量验收：工具调用日志筛选分页与 CSV 导出

- [x] `GET /tool-call-logs` 支持 `agent_id`、`tool_name`、`status`、`from`、`to`、`limit`、`cursor` 查询参数。
- [x] 响应返回 `items` 与 `next_cursor`，继续复用 `pkg/response`。
- [x] 新增 `GET /tool-call-logs/export`，按当前筛选条件导出 CSV，单次最多 1000 条。
- [x] 工具调用日志查询与导出固定按当前 `X-Tenant-ID` 租户上下文隔离，不接受客户端覆盖 `tenant_id`。
- [x] 新增 `000021_tool_call_logs_filter_indexes` migration up/down。
- [x] 新增 `ToolCallLogQuery` 归一化/校验、cursor 编解码和时间解析单元测试。
- [x] 管理台“工具调用日志”区新增筛选栏与“导出 CSV”按钮，中文摘要展示，无黑色 JSON 原文区域。
- [x] 本地 `gofmt` 通过；`node --check 02_frontend/assets/app.js` 通过。
- [ ] `go test ./...`（本地沙箱无 Go 1.26.3 工具链且网络受限，未执行；需在具备 Go 1.26.3 的环境验证）。


## 2026-05-31 增量验收：MCP 网关安全策略与连通性 dry-run

- [x] 新增 `GET /mcp-gateway/policy`，默认 `enabled=false`、`default_action=deny`、`audit_action=agent.mcp.call`、`danger_confirmation=true`。
- [x] 新增 `GET /mcp-servers`，返回只读服务（kb_resource、agent_catalog）与危险服务（external_http、local_stdio）目录及传输方式。
- [x] 新增 `POST /mcp-servers/{server_id}/test` 连通性 dry-run，只读返回 `dry_run_ok`、危险返回 `blocked`，均不建立真实连接。
- [x] MCP 测试复用 `tool_call_logs`，以 `mcp:<server_code>` 写入 dry-run 审计，仅保存摘要不保存敏感明文。
- [x] `POST /mcp-servers/{server_id}/test` 走 `RequireTenantWriter` 权限中间件，按当前 `X-Tenant-ID` 租户隔离。
- [x] 未新增数据库表，纯函数策略 + dry-run，零真实出站连接（防 SSRF/数据外泄）。
- [x] 新增 MCP 策略/目录/dry-run 单元测试（默认安全、目录去重、阻断危险服务、放行只读服务）。
- [x] 管理台新增“MCP 网关安全策略”卡片（策略摘要 + 服务目录 + 连通性测试），中文摘要展示，无黑色 JSON 原文。
- [x] 本地 `gofmt` 通过；`node --check 02_frontend/assets/app.js` 通过。
- [ ] `go test ./...`（本地沙箱无 Go 1.26.3 工具链且网络受限，未执行；需在具备 Go 1.26.3 的环境验证）。


## 2026-05-31 增量验收：工具 Tool Schema 与参数校验

- [x] `GET /tools` 每个工具新增 `input_schema`（name/type/required/description/example）。
- [x] `kb_search`、`file_lookup`、`kb_mutation`、`billing_operation` 均提供参数 Schema。
- [x] `POST /tools/{id}/test` 返回新增 `schema_valid` 与 `schema_issues`，对缺失必填参数与未声明参数给出中文提示。
- [x] Schema 校验为信息性提示，不改变安全语义（只读仍 `dry_run_ok`、危险仍 `blocked`）。
- [x] 管理台工具卡片展示“参数 Schema”，安全测试按 Schema 构造示例入参并展示校验结果。
- [x] 新增 `toolInputSchema`/`validateToolInput`/`BuildToolTestResult` schema 单元测试。
- [x] 本地 `gofmt` 通过；`node --check 02_frontend/assets/app.js` 通过。
- [ ] `go test ./...`（本地沙箱无 Go 1.26.3 工具链且网络受限，未执行；需在具备 Go 1.26.3 的环境验证）。


## 2026-05-31 增量验收：插件市场（启用/禁用持久化）

- [x] 新增 `000022_plugin_installs` 迁移（表 + 唯一约束 + 索引 + down 回滚）。
- [x] 新增 `GET /plugins`，返回插件目录（合并租户启用状态）与市场安全策略。
- [x] 新增 `POST /plugins/{plugin_code}/enable` 与 `/disable`，`blocked` 插件不可启用（409）。
- [x] 启用/禁用走 `RequireTenantWriter`，按当前 `X-Tenant-ID` 租户隔离，upsert 持久化。
- [x] 内置插件：kb_search_plugin、genealogy_insight_plugin、webhook_notify_plugin、web_search_plugin。
- [x] 新增插件目录/合并/策略纯函数单元测试（默认状态、租户覆盖、blocked 不可安装）。
- [x] 管理台新增“插件市场”卡片，支持一键启用/禁用，中文摘要展示。
- [x] 本地 `gofmt` 通过；`node --check 02_frontend/assets/app.js` 通过。
- [ ] `go test ./...`（本地沙箱无 Go 1.26.3 工具链且网络受限，未执行；需在具备 Go 1.26.3 的环境验证）。


## 2026-05-31 增量验收：工作流编排（节点类型目录与图结构校验）

- [x] 新增 workflow 模块（types/handler/routes），在 bootstrap 注册到 tenantScoped 路由组。
- [x] `GET /workflows/orchestration-policy` 返回安全默认策略（engine 默认关闭、validate_only）。
- [x] `GET /workflow-node-types` 返回内置节点类型目录。
- [x] `POST /workflows/validate` 做图结构校验：起止节点、边引用、出边规则、环检测与拓扑执行顺序。
- [x] 图校验为纯函数，不执行真实节点动作，不新增数据库表。
- [x] `POST /workflows/validate` 走 RequireTenantWriter，按当前 X-Tenant-ID 租户隔离，节点上限 100。
- [x] 新增图校验算法单元测试（合法/缺 start/缺 end/环/悬空边/类型非法/end 出边/策略默认）。
- [x] 管理台新增“工作流编排”面板（策略 + 节点类型 + 图定义校验输入与中文诊断）。
- [x] 本地 `gofmt` 通过；`node --check 02_frontend/assets/app.js` 通过。
- [ ] `go test ./...`（本地沙箱无 Go 1.26.3 工具链且网络受限，未执行；需在具备 Go 1.26.3 的环境验证）。


## 2026-05-31 增量验收：工作流定义持久化（草稿/发布/归档）

- [x] 新增 `000023_workflows` 迁移（表 + 唯一约束 + 部分索引 + down 回滚）。
- [x] 新增 workflow Repository（Create/List/Get/Update/SetStatus/Archive），镜像 Agent CRUD 模式。
- [x] `GET /workflows`、`GET /workflows/{id}`、`POST /workflows`、`PUT /workflows/{id}`、`POST /workflows/{id}/publish`、`DELETE /workflows/{id}`。
- [x] 创建/更新/发布写入前复用 ValidateWorkflowGraph 做图结构校验，不通过返回 40046。
- [x] 写操作走 RequireTenantWriter，按 X-Tenant-ID 租户隔离；code 租户内唯一（重复 409）。
- [x] 已发布工作流更新后回到草稿并递增版本号。
- [x] 新增创建/更新请求归一化校验与 marshalDefinition 单元测试。
- [x] 管理台“工作流编排”面板新增工作流定义列表、创建表单与发布/归档操作。
- [x] 本地 `gofmt` 通过；`node --check 02_frontend/assets/app.js` 通过。
- [ ] `go test ./...`（本地沙箱无 Go 1.26.3 工具链且网络受限，未执行；需在具备 Go 1.26.3 的环境验证）。


## 2026-05-31 增量验收：工作流执行引擎（dry-run）与执行日志

- [x] 新增 `000024_workflow_runs` 迁移（表 + 租户/工作流时间索引 + down 回滚）。
- [x] 新增 `SimulateWorkflowRun` 纯函数：拓扑顺序模拟，llm/tool 标记 simulated，human_approval 暂停，其后 pending。
- [x] `POST /workflows/{id}/run`：dry-run 模拟并写入执行日志，校验失败整体 blocked，不执行真实动作。
- [x] `GET /workflows/{id}/runs`：返回执行日志列表，按时间倒序、租户隔离。
- [x] Repository InsertWorkflowRun/ListWorkflowRuns，input/steps 以 JSONB 存取。
- [x] run 走 RequireTenantWriter；草稿与已发布均可 dry-run（归档已排除）。
- [x] 新增 SimulateWorkflowRun 单元测试（线性完成 / 人工确认暂停 / 非法图阻断）。
- [x] 管理台工作流定义列表新增“试运行”按钮，新增“运行记录（执行日志）”展示区。
- [x] 本地 `gofmt` 通过；`node --check 02_frontend/assets/app.js` 通过。
- [ ] `go test ./...`（本地沙箱无 Go 1.26.3 工具链且网络受限，未执行；需在具备 Go 1.26.3 的环境验证）。


## 2026-05-31 增量验收：Agent 渠道接入（Web/H5/API 接入点）

- [x] 新增 channel 模块（types/repository/handler/routes），在 bootstrap 注册到 tenantScoped。
- [x] 新增 `000025_agent_channels` 迁移（表 + 唯一约束 + 部分索引 + down 回滚），channel_key 由库默认生成。
- [x] `GET /channel-types` 返回渠道类型目录（web/h5/api 可用，wechat_official/wechat_work 待对接）。
- [x] `GET /channels`、`GET /channels/{id}`、`POST /channels`、启用/禁用、`DELETE`（归档）。
- [x] 写操作走 RequireTenantWriter，按 X-Tenant-ID 租户隔离；agent FK 缺失返回 404，channel_key 冲突 409。
- [x] 创建仅允许 active 渠道类型；归档为软删除。
- [x] 新增渠道类型目录与创建请求校验单元测试。
- [x] 管理台新增“渠道接入”面板（类型目录 + 新建渠道表单 + 列表与启用/禁用/删除，展示 channel_key）。
- [x] 本地 `gofmt` 通过；`node --check 02_frontend/assets/app.js` 通过。
- [ ] `go test ./...`（本地沙箱无 Go 1.26.3 工具链且网络受限，未执行；需在具备 Go 1.26.3 的环境验证）。


## 2026-05-31 增量验收：渠道接入代码生成

- [x] 新增 `GET /channels/{channel_id}/embed`，按渠道类型生成接入代码与中文说明。
- [x] `web` 生成 `<script>` 嵌入片段，`h5` 生成页面链接，`api` 生成 curl 示例。
- [x] baseURL 按当前请求 scheme+host 推断；渠道未启用时追加“启用后生效”提示。
- [x] `BuildChannelEmbed` 为纯函数，不新增数据库表、不访问网络。
- [x] 按当前 X-Tenant-ID 租户隔离（复用 Get），渠道不存在返回 404。
- [x] 新增 BuildChannelEmbed 单元测试（web/api/disabled）。
- [x] 管理台渠道列表新增“接入代码”按钮，点击后展示嵌入片段、API 端点与说明。
- [x] 本地 `gofmt` 通过；`node --check 02_frontend/assets/app.js` 通过。
- [ ] `go test ./...`（本地沙箱无 Go 1.26.3 工具链且网络受限，未执行；需在具备 Go 1.26.3 的环境验证）。


## 2026-05-31 增量验收：渠道公开连接端点

- [x] 新增 `GET /channel-connect/{channel_key}` 公开端点（仅 IP 限流，不经登录/租户中间件）。
- [x] Repository 新增 `GetByChannelKey`，按 channel_key 查询 status=enabled 且未软删除的渠道。
- [x] 返回接入所需最小信息（channel_key/type/name/agent_id/status/connected），不泄露其它租户数据。
- [x] 渠道不存在或未启用返回 404；channel_key 为空返回 400。
- [x] bootstrap 用独立公开组（IP 限流）注册，复用现有 channelHandler。
- [x] 公开端点使用独立前缀 /channel-connect/，与 /channels/:channel_id 零路由冲突。
- [x] 本地 `gofmt` 通过。
- [ ] `go test ./...`（本地沙箱无 Go 1.26.3 工具链且网络受限，未执行；需在具备 Go 1.26.3 的环境验证）。


## 2026-05-31 增量验收：渠道更新（补全 CRUD）

- [x] 新增 `PUT /channels/{channel_id}`，更新渠道名称与配置（名称/配置至少其一）。
- [x] Repository `Update` 镜像 workflow.Update：先 Get 取原值，未提供字段保持原值，租户隔离。
- [x] 走 RequireTenantWriter；渠道不存在返回 404。
- [x] 新增 `UpdateChannelRequest` 归一化/校验单元测试（改名 / 仅改配置 / 空请求报错）。
- [x] 管理台渠道列表新增“重命名”按钮（弹窗输入新名称后更新）。
- [x] 本地 `gofmt` 通过；`node --check 02_frontend/assets/app.js` 通过。
- [ ] `go test ./...`（本地沙箱无 Go 1.26.3 工具链且网络受限，未执行；需在具备 Go 1.26.3 的环境验证）。


## 2026-05-31 增量验收：渠道概览统计

- [x] 新增纯函数 `SummarizeChannels`：聚合总数、启用/禁用数与按类型分布。
- [x] 新增 `GET /channels/summary`，复用 List 后做纯函数聚合，按 X-Tenant-ID 隔离。
- [x] 路由 `/channels/summary`（静态）与 `/channels/:channel_id`（参数）共存，注册顺序静态在前。
- [x] 新增 `SummarizeChannels` 单元测试（计数与按类型分布 / 空列表）。
- [x] 管理台“渠道接入”面板顶部新增概览卡片（总数/启用/禁用/类型分布）。
- [x] 本地 `gofmt` 通过；`node --check 02_frontend/assets/app.js` 通过。
- [ ] `go test ./...`（本地沙箱无 Go 1.26.3 工具链且网络受限，未执行；需在具备 Go 1.26.3 的环境验证）。


## 2026-05-31 增量验收：工作流概览统计

- [x] 新增纯函数 `SummarizeWorkflows`：聚合总数、草稿/已发布数与按状态分布。
- [x] 新增 `GET /workflows/summary`，复用 List 后做纯函数聚合，按 X-Tenant-ID 隔离。
- [x] 路由 `/workflows/summary`（静态）与 `/workflows/:workflow_id`（参数）共存，静态在前注册。
- [x] 新增 `SummarizeWorkflows` 单元测试（计数与按状态分布 / 空列表）。
- [x] 管理台“工作流编排”面板新增概览卡片（总数/草稿/已发布）。
- [x] 本地 `gofmt` 通过；`node --check 02_frontend/assets/app.js` 通过。
- [ ] `go test ./...`（本地沙箱无 Go 1.26.3 工具链且网络受限，未执行；需在具备 Go 1.26.3 的环境验证）。


## 2026-05-31 增量验收：P1 只读端点功能冒烟脚本

- [x] 新增 `01_backend/deploy/production/scripts/smoke-features.sh`，带鉴权冒烟测试。
- [x] 覆盖工具安全策略/工具目录/工具调用日志/MCP 网关/插件市场只读端点。
- [x] 覆盖工作流编排策略/节点类型/列表/概览统计端点。
- [x] 覆盖渠道类型目录/列表/概览统计端点。
- [x] 需要 `TOKEN` 与 `TENANT_ID` 环境变量；未设置时跳过并以 0 退出（不阻断流水线）。
- [x] 每个端点断言 HTTP 200 且 `application/json`，失败即非零退出。
- [x] 本地 `bash -n` 语法校验通过；脚本已置可执行位。
- [ ] 实际运行需在部署环境提供有效 `TOKEN`/`TENANT_ID`（本沙箱无运行服务，未实际执行）。


---

# 附：P1 增量验证与合并手册（2026-05-31）

本批 P1 增量在交付沙箱中受限于 Go 版本（仅 1.25.1，`go.mod` 要求 1.26.3）与受限网络，**未执行 `go build` / `go test`**，仅通过 `gofmt` 解析与 `node --check` 校验。请在具备完整工具链的环境按本手册验证后再合并。

## 1. 适用范围

整合分支 `feat/channel-update`，整合 PR **#12（→ master）**；原子 PR #1~#11。涵盖：
- 插件工具：工具调用日志筛选/CSV 导出（000021）、MCP 网关 dry-run、Tool Schema、插件市场（000022）
- 工作流：图校验、定义持久化（000023）、dry-run 执行引擎+执行日志（000024）、概览统计
- 渠道入口：接入点 CRUD（000025）、接入代码、公开连接端点、概览统计
- 运维：P1 只读端点功能冒烟脚本

## 2. 环境要求

Go 1.26.3；PostgreSQL 16 + pgvector、Redis 7.4、MinIO/S3；可访问 Go module 代理。

## 3. 编译与单元测试

```bash
cd 01_backend
go build ./...
go test ./...
```

重点测试包：`internal/module/agent`、`internal/module/workflow`、`internal/module/channel`。

## 4. 数据库迁移

```bash
go run ./cmd/migrate    # 或 make migrate
```

新增表：`plugin_installs`、`workflows`、`workflow_runs`、`agent_channels`；筛选索引：000021。各迁移均含 `*.down.sql` 可逐级回滚。

## 5. 启动与路由验证

```bash
go run ./cmd/server
```

确认无路由注册 panic：`workflow`/`channel` 已注册到 `tenantScoped`；公开端点 `GET /api/v1/channel-connect/:channel_key` 在独立公开组（IP 限流）；静态与参数路由共存（`/workflows/summary` 与 `/workflows/:workflow_id`、`/channels/summary` 与 `/channels/:channel_id`）。

## 6. 功能冒烟（只读端点）

```bash
export API_BASE_URL="https://your-domain/saas-api/api/v1"
export TOKEN="<登录获取的 JWT>"
export TENANT_ID="<目标租户 ID>"
bash deploy/production/scripts/smoke-features.sh
```

覆盖 13 个只读端点，逐个断言 HTTP 200 + `application/json`。

## 7. 关键写操作手测（示例）

```bash
# 工作流图校验（不落库）
curl -sS -X POST "$API_BASE_URL/workflows/validate" \
  -H "Authorization: Bearer $TOKEN" -H "X-Tenant-ID: $TENANT_ID" -H 'Content-Type: application/json' \
  -d '{"definition":{"nodes":[{"id":"s","type":"start"},{"id":"e","type":"end"}],"edges":[{"from":"s","to":"e"}]}}'

# 创建并发布工作流，再试运行：POST /workflows -> POST /workflows/{id}/publish -> POST /workflows/{id}/run -> GET /workflows/{id}/runs
# 创建渠道并取接入代码：POST /channels -> GET /channels/{id}/embed -> GET /channel-connect/{channel_key}
```

## 8. 合并建议

- 推荐：验证全部通过后直接合并整合 PR **#12**。
- 或：按 #1 → … → #11 顺序逐个合并堆叠 PR（合并上游后下游自动重定向）。

## 9. 风险与注意事项

- 工具/MCP/工作流执行均为「安全默认 + dry-run」，当前不执行真实外部动作；启用真实执行前需补齐权限校验、人工确认与审计。
- 公开端点 `/channel-connect/:channel_key` 仅返回最小信息且加 IP 限流，`channel_key` 为随机 UUID。
- 所有写操作走 `RequireTenantWriter` 并按 `X-Tenant-ID` 隔离。
- 若验证暴露问题，可在对应原子 PR 修复后重验，不影响其它增量。


## 2026-05-31 增量验收：工作流复制（克隆为新草稿）

- [x] 新增纯函数 `DuplicateWorkflowRequest`：名称追加"副本"、编码追加 `-copy-<后缀>` 且不超过 64 字符、定义整体复制。
- [x] 新增 `POST /workflows/{id}/duplicate`：基于源工作流 Create 一个新草稿，按 X-Tenant-ID 隔离、走 RequireTenantWriter。
- [x] 新增 `DuplicateWorkflowRequest` 单元测试（名称/编码/定义复制、长编码截断到 ≤64）。
- [x] 管理台工作流列表新增"复制"按钮。
- [x] **Go 1.25 实测**：workflow 纯函数包 `go vet`+`go test` 通过（含新增复制逻辑）。
- [x] 本地 `gofmt` 通过；`node --check 02_frontend/assets/app.js` 通过。
- [ ] 含第三方依赖的完整 `go build/test ./...` 须在联网/vendor 环境验证。
